{
  "name": "failable",
  "version": "0.0.1",
  "description": "Provides the Failable<T> type for success states",
  "main": "lib/index.js",
  "typings": "lib/index",
  "scripts": {
    "pretest": "typings install",
    "test": "mocha --require ts-node/register ./src/**/*.spec.ts",
    "prebuild": "rm -rf lib && typings install",
    "build": "tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UrbanDoor/failable.git"
  },
  "author": "kourge",
  "private": true,
  "bugs": {
    "url": "https://github.com/UrbanDoor/failable/issues"
  },
  "homepage": "https://github.com/UrbanDoor/failable#readme",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "ts-node": "^0.7.1",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}
